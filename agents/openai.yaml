interface:
  display_name: "/xxd-panel-053"
  short_description: "Create Panel 053 from its original style brief."
  default_prompt: "Use $xxd-panel-053; treat its bundled original style brief as the sole aesthetic authority, and ask only for unresolved mode, size, text mode, locale, or wallpaper settings."

policy:
  allow_implicit_invocation: true
