interface:
  display_name: "/xxd-panel-053"
  short_description: "Create Panel 053 from its original style brief."
  default_prompt: "Use $xxd-panel-053; keep the body of its bundled original style brief as the sole aesthetic authority, let the image model compose each complete canvas without fixed panel boxes unless I request exact geometry, and keep prompt-generated wording rooted in the current source image."

policy:
  allow_implicit_invocation: true
